apiVersion: v1
kind: Service
metadata:
  name: my-python-app-service
spec:
  selector:
    app: my-python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001  # Replace with your desired NodePort
  type: NodePort  # Expose via NodePort instead of LoadBalancer
